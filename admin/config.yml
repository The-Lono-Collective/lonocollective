backend:
  name: github
  repo: The-Lono-Collective/lonocollective
  branch: main
  client_id: Ov23lihVtW4IpcC39zQc
  base_url: https://auth.lonocollective.ai

# Sveltia CMS does not yet support editorial_workflow. With this disabled,
# saves publish directly to the `main` branch — no draft PR review step.
# Re-enable if returning to Decap.
# publish_mode: editorial_workflow

media_folder: assets/images/blog
public_folder: /assets/images/blog

collections:
  - name: posts
    label: Blog Posts
    folder: _posts
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - { label: Layout, name: layout, widget: hidden, default: post }
      - { label: Title, name: title, widget: string }
      - {
          label: Date,
          name: date,
          widget: datetime,
          date_format: YYYY-MM-DD,
          time_format: false,
        }
      - label: Category
        name: category
        widget: select
        options:
          - { label: Research, value: research }
          - { label: Announcement, value: announcement }
      - label: Author
        name: author
        widget: relation
        collection: authors
        search_fields: [name]
        value_field: "{{slug}}"
        display_fields: [name]
      - {
          label: Excerpt,
          name: excerpt,
          widget: text,
          hint: "One or two sentences shown on the blog index. Write this yourself — don't let it auto-truncate.",
        }
      - {
          label: Cover Image,
          name: cover_image,
          widget: image,
          required: false,
          hint: "Shown as the hero at the top of the post and as the social-share preview image.",
        }
      - {
          label: Tags,
          name: tags,
          widget: list,
          required: false,
          hint: "Freeform topic tags (e.g. LLM, regulation). Readers can filter the blog by these.",
        }
      - { label: Body, name: body, widget: markdown }

  - name: authors
    label: Authors
    folder: _data/authors
    create: true
    identifier_field: name
    extension: yml
    format: yaml
    fields:
      - { label: Name, name: name, widget: string }
      - {
          label: URL,
          name: url,
          widget: string,
          required: false,
          hint: "Team profile anchor (e.g. /#team) or external URL. Leave blank for name-only attribution.",
          pattern: ['^(https?://|/|#).*|^$', "Must be a relative path, anchor, or http(s) URL"],
        }
      - {
          label: Bio,
          name: bio,
          widget: text,
          required: false,
          hint: "One or two sentences. Appears at the bottom of every post you write.",
        }
      - {
          label: Photo,
          name: image,
          widget: image,
          required: false,
          hint: "Headshot shown beside your bio.",
        }
